# Install a CUDA-enabled PyTorch + TorchAudio build for the local NVIDIA driver
# before this file. See README.md; letting pip choose a CPU wheel wastes RTX 4050.
demucs>=4.0,<5
easyocr>=1.7,<2
edge-tts>=6.1,<8
fastapi>=0.110,<1
faster-whisper>=1.0,<2
ffmpeg-python>=0.2,<1
huggingface-hub>=0.24,<1
httpx>=0.27,<1
numpy>=1.23.5,<2
Pillow>=9.2,<13
opencv-python-headless==4.10.0.84
pydub>=0.25,<1
python-multipart>=0.0.9,<1
python-telegram-bot>=20.7,<23
requests>=2.31,<3
srt>=3.5,<4
deep-translator>=1.11,<2
uvicorn[standard]>=0.29,<1
yt-dlp>=2024.4
# Optional browser-profile bridge used to export only Douyin cookies through
# Chrome's own decryption context. It uses the installed browser binary.
playwright>=1.40,<2
gmssl>=3.2.2,<4

# Keep the main bot environment compatible with the pinned CUDA Torch and
# current Hugging Face client. Strong-model dependencies belong in model_venv.
sympy==1.13.1
click>=8.1.7,<8.2

# Optional voice-clone providers used only when selected/configured:
# capcut-tts-api
# rvc-python
